# CONTINUITY.md

- Last Updated (UTC): 2026-09-23T06:55:00Z
- Last Agent Stamp: 2026-09-22T13:48:24Z | GPT-5 (Codex) | account=unknown

- Goal (incl. success criteria):
  - Актуальный запрос: исправить реальную публикацию фото на `/aktuelles` после Telegram confirm: ссылка и Docker build есть, но браузер показывает битое изображение. Success: публичный WebP возвращает HTTP 200, будущие загрузки получают доступные nginx права, bot не объявляет success при недоступном asset.
  - Актуальный запрос: синхронизировать локальный проект с GitHub, установить подтверждённую причину, по которой Telegram-бот отвечает `Erledigt`, но не публикует присланное изображение на `/aktuelles`, исправить runtime-цепочку и проверить результат.
  - Success: локальная `main` соответствует `origin/main`; photo/document flow не выдаёт ложного успеха, сохраняет WebP и ссылку в разрешённых файлах, проходит typecheck/lint/build и, если production-доступ подтверждён безопасно, развёрнут и проверен end-to-end.
  - Актуальный запрос: заменить Codex в роли исполнительного AI-агента Telegram-бота на AGY (Google Antigravity CLI), сохранив Telegram-вход, авторизацию, безопасный image pipeline, diff/confirm, deploy и rollback.
  - Success: production-бот вызывает AGY 1.2.7 headless под отдельным непривилегированным пользователем; модель явно закреплена; результат и ошибки машинно проверяются; AGY ограничен workspace/sandbox; Codex не участвует в runtime; typecheck, AGY smoke, service/webhook/health и сценарий изменения контента проверены.
  - Актуальный запрос: проверить, работает ли production Telegram-бот, выяснить причину отказа пользователю при замене изображения на главной странице и расширить функциональность для безопасной корректной замены изображений с последующим production-деплоем.
  - Success: подтверждена вся runtime-цепочка bot/webhook/auth/Codex/deploy; бот принимает поддерживаемые изображения без ослабления файловой безопасности, создаёт совместимые ассеты/ссылки, сайт проходит build и визуально/HTTP корректен после подтверждения; изменения протестированы и развёрнуты.
  - Актуальный запрос: восстановить доступ Telegram-пользователя с доверенным номером `+49 177 7711246` и корректно развернуть исправление production-бота.
  - Success: номер не дублируется в allowlist; для Telegram user id `6327018818` бот устойчиво показывает кнопку передачи собственного контакта, принимает только self-contact, после деплоя service/webhook/health проверены и доступ подтверждён журналом либо реальным user-action.
  - Актуальный запрос: подготовить воспроизводимый rollback-safe script и самостоятельно выполнить production-восстановление Codex Telegram-бота на SherMos2, отслеживая все ошибки до systemd, health и CLI smoke-проверок.
  - Success: script делает preflight/backup/автоматический rollback при ошибке, заменяет Snap-wrapper на официальный standalone Codex под `ubuntu`, исправляет Git ownership, проверяет вызов из service-like systemd cgroup, typecheck и active HTTPS health; результат и backup path зафиксированы без secrets.
  - Актуальный запрос: не вносить production-изменения, а подготовить заранее проверенную последовательность команд для устранения Snap/cgroup-сбоя Codex и вторичного дефекта прав Git; отдельно подготовить поэтапный план, в котором клиентский AWS account становится management/payer через AWS Organizations.
  - Success: команды имеют preflight, backup/rollback, не раскрывают secrets, устанавливают официальный Codex вне Snap, запускают Git/Codex от `ubuntu`, проверяют service-context и реальный Telegram E2E; AWS Organizations план явно разделяет приглашение, принятие, консолидированную оплату, IAM-доступ, бюджеты и выход/откат.
  - Актуальный запрос: детально переисследовать проект; read-only диагностировать, почему Telegram-бот снова не отвечает; через явно выбранный Chrome проверить текущий AWS Billing, срочность и сумму оплаты; по официальным данным определить, можно ли перевести оплату на немецкого клиента и как безопасно это оформить.
  - Success: причина сбоя подтверждена свежими локальными и production-доказательствами без раскрытия secrets; AWS суммы и сроки сняты из текущей авторизованной консоли; варианты передачи оплаты отделяют payment method от account ownership/tax/legal responsibility; три независимых кандидата подготовлены, а невозможность live `llm-verifier` зафиксирована без ложного заявления о выбранном победителе.
  - Пользователь явно разрешил production-замену ошибочного `+7 708…6841` на фактический Telegram-номер `+7 707…5718`; сохранить три немецких номера, сделать backup, restart и verify.
  - Текущая проверка allowlist: подтвердить наличие трёх явно названных немецких телефонов и сформировать безопасный итоговый набор вместе с фактическим Telegram-номером пользователя.
  - Текущий диагноз: self-contact пользователя отклонён как `phone_not_allowed`; подтвердить расхождение с allowlist и не расширять административный доступ без явного разрешения владельца.
  - Текущий шаг: подтвердить доставку показанного пользователем `/start`, провести self-contact авторизацию и определить формат двуязычных немецко-русских ответов.
  - Текущая проверка: подтвердить production-доступность Telegram-бота свежими данными сервера и предоставленного токена; затем read-only проверить AWS Billing — сумму, срок оплаты и риск блокировки.
  - Текущий production-инцидент: Telegram-бот фактически не отвечает; заново проверить всю цепочку до реального входящего сообщения и не считать восстановление успешным по одному локальному health.
  - Текущая фаза: безопасно проверить добавленные Telegram/AssemblyAI ключи, запустить Telegram-редактор, зарегистрировать webhook и проверить реальную транскрибацию.
  - Восстановить Telegram-редактор на `SherMos2`: четыре разрешённых телефона, Telegram webhook, Codex-редактирование, подтверждение diff и голосовая транскрибация.
  - Спроектировать полный уход от 1blu: перенос `.de`-регистратора, DNS, почты и стабильный AWS runtime без простоя.
  - Success: env/runtime подготовлены без раскрытия секретов; доступные интеграции проверены; блокирующие ключи и провайдеры названы; перенос домена разделён на обратимые фазы.
- Constraints/Assumptions:
  - В этой фазе не менять AWS-регистратора, DNS и схему размещения домена; переход на Amazon отложен по прямому указанию владельца.
  - Язык общения и коммитов — русский. Контент сайта — `site/src/data/content.js`.
  - Значения secrets не выводятся в терминал/чат; проверяются только наличие, формат и API-ответы.
  - Текущий production DNS/почта не меняются до отдельной согласованной фазы миграции.
  - Перед каждым нетривиальным выполнением сначала обновлять `functions.update_plan` и этот ledger; план сохраняется через сжатие контекста.
- Key decisions:
  - Image-edit architecture: Telegram bytes are validated and converted by the bot into a unique WebP under `site/public/images/admin/`; AGY decides where to reference the prepared public URL. The generated asset participates in allowlist/commit and is explicitly removed on failure, cancel, replacement or timeout.
  - AGY prompt передаётся через NDJSON stdin, чтобы текст администратора и история не попадали в process list; terminal result принимается только при JSON status `SUCCESS`.
  - Текущий Telegram flow: webhook → self-contact auth → SQLite user_id → AGY CLI → разрешённый git diff → подтверждение → commit/rebuild.
  - AGY запускается через root-owned wrapper как `ubuntu`, с pinned `gemini-3.8-flash-medium`, `--sandbox`, `accept-edits`, явным `--add-dir`; `--dangerously-skip-permissions` запрещён.
  - Минимальное восстановление транскрибации использует AssemblyAI; вариант полной AWS-консолидации — Amazon Transcribe + EC2 IAM role, без долгоживущего API key.
  - Практичный уход от 1blu: перенести регистрацию `.de` в Route 53, а DNS можно временно оставить в бесплатном Cloudflare. Полный AWS DNS требует Route 53/CloudFront или прямого EC2 origin.
  - Amazon WorkMail не выбран: AWS объявил завершение поддержки 2027-03-31. SES не является обычным IMAP/POP mailbox.
- State:
  - Done:
    - Production сайта восстановлен на EC2 `i-0faf9efc1b3f85145`, EIP `63.186.147.213`; Cloudflare активен, почта пока на 1blu.
    - На SherMos2 создан backup `/etc/music_school.env.bak-20260721-172210`; основной `/etc/music_school.env` сохранён как `root:ubuntu`, mode `640`.
    - В `TELEGRAM_ALLOWED_PHONES` добавлены четыре нормализованных номера; новый `TELEGRAM_WEBHOOK_SECRET` сгенерирован, но не раскрыт.
    - Подготовлены остальные env-пути; `TRANSCRIPTION_LANGUAGE=auto`; `TELEGRAM_BOT_TOKEN` и `ASSEMBLYAI_API_KEY` оставлены пустыми как реальные блокеры.
    - Установлен ffmpeg; Node 20/npm найдены; `npm ci`, `npm audit` (0 vulnerabilities) и `npm run typecheck` прошли.
    - Создан `/etc/systemd/system/musikschule-tg-bot.service`; unit оставлен disabled/inactive до добавления ключей.
    - Локальный HTTPS smoke на временном порту прошёл: `/health` → ok, загружены 4 phone entries; порт после теста закрыт.
    - Codex CLI под `ubuntu` авторизован через ChatGPT; root-wrapper `/usr/local/bin/music-school-codex` проверен; read-only Codex smoke → `OK`.
    - GitHub SSH пользователя `ubuntu` проверен для `mrMoses2000`; server worktree чистый.
    - Старый SherMos1 по SSH недоступен; на SherMos2, локально и в Git не найдено копий Telegram/AssemblyAI secrets или прежней SQLite DB.
    - Подтверждено официальной документацией: Route 53 поддерживает регистрацию/transfer `.de`; нужен AuthInfo, доступ к registrant email и немецкий resident/admin contact; DNS должен пройти DENIC zone check.
    - Добавленные Telegram/AssemblyAI secrets проверены без раскрытия: Telegram `getMe` → HTTP 200 для `@music_site_admin_bot`, AssemblyAI list API → HTTP 200.
    - `musikschule-tg-bot.service` включён и активен; локальный HTTPS health успешен, порт `0.0.0.0:8443` слушается, загружены 4 phone-auth entries.
    - Сквозной voice smoke выявил снятую с поддержки модель `universal`; код обновлён на `universal-3-5-pro` с fallback `universal-2`, typecheck прошёл, commit `2cf7dd4` запушен и развёрнут.
    - Повторный сквозной тест `ffmpeg → AssemblyAI upload/submit/poll → text` успешен: непустой текст, 4951 символ, 26 секунд.
    - Telegram уже хранит правильный webhook URL `https://musikschule-cms-bielefeld.de:8443/api/telegram/webhook`, pending updates 0, last error отсутствует.
    - Повторная диагностика 2026-08-04: EC2 и systemd активны, TLS валиден, `8443` доступен через Cloudflare и напрямую, Telegram/AssemblyAI env присутствуют; SQLite содержала 0 пользователей и 0 сообщений.
    - Фактический разрыв был на контракте webhook secret: URL существовал, но приложение молча отвечало HTTP 200 запросам с несовпадающим secret, поэтому Telegram не показывал ошибку, а update терялись.
    - Webhook заново зарегистрирован с текущим server secret; Telegram API ответил `Webhook was set`.
    - В commit `4d92c5c` добавлены автоматическая идемпотентная регистрация webhook при старте, HTTP 403/лог при неверном secret и лог принятого update; локальный и серверный typecheck прошли, commit развёрнут.
    - Production restart успешен: `setWebhook result: true`, health ok, неподписанный POST → 403; подписанный synthetic update прошёл Cloudflare и обработчик до ожидаемой ошибки `chat not found`.
    - Свежая проверка 2026-08-11: SSH доступен; `musikschule-tg-bot.service` active/enabled, uptime около 6.8 суток, restart count 0; локальный и публичный `/health` успешны, `0.0.0.0:8443` слушается.
    - Предоставленный владельцем token совпадает с production env по SHA-256; Telegram `getMe` и `getWebhookInfo` вернули HTTP 200, webhook URL корректен, pending updates 0, last error отсутствует; AssemblyAI API вернул HTTP 200.
    - SQLite всё ещё содержит 0 авторизованных пользователей и 0 входящих сообщений: инфраструктура бота исправна, но реальный пользовательский `/start` и self-contact auth не выполнены и end-to-end доступ для клиента пока не доказан.
    - AWS Billing проверен read-only через Chrome и визуально через Computer Use: outstanding balance `$0.00`, payments due `0`, текущий bill pending с estimated total `$1.14`; raw month-to-date `$8.15`, monthly forecast `$25.12`.
    - AWS credit: фактически осталось `$7.01`, прогнозируемый остаток `$0.00`, срок до 2027-01-22. Default Visa действует до 2028-12; backup payment method отключён; текущего предупреждения о suspension или назначенной due date нет.
    - Первый реальный `/start` подтверждён production-журналом: webhook принял update и корректно направил ещё не авторизованного пользователя в self-contact flow. После этого SQLite всё ещё содержит 0 авторизованных пользователей и 0 сообщений.
    - Реальный self-contact update также принят; production log зафиксировал отказ `phone_not_allowed`. Сверка без раскрытия allowlist подтвердила: фактического номера Telegram с префиксом `+7 707` нет, ранее указанный номер с префиксом `+7 708` присутствует; service остаётся active.
    - Три явно названных немецких номера отдельно проверены на SherMos2: все уже присутствуют в `TELEGRAM_ALLOWED_PHONES`; повторное добавление не требуется, service active.
    - По явному разрешению владельца ошибочный `+7 708…6841` заменён в production env на фактический Telegram-номер `+7 707…5718`; timestamped backup `/etc/music_school.env.bak-20260811-115715` создан, итоговый allowlist содержит ровно 4 номера: фактический +7 и три немецких.
    - После env-замены права остались `root:ubuntu 640`; service active/enabled, local/public health ok, startup `setWebhook result: true`, Telegram webhook URL корректен, pending 0, last error отсутствует. Авторизация ещё 0 до повторного self-contact.
    - Повторная полная индексация 2026-08-23: 373 файла, git `967adc4`; каталог `music_site_copy/`, заявленный источником истины, отсутствует, поэтому сверка контента 1:1 недоступна.
    - Локальные проверки: Telegram bot `npm ci`, audit (0 vulnerabilities) и typecheck успешны; сайт lint/build успешны. Свежий audit сайта: 2 high production advisories (`react-router`/`react-router-dom`, RSC-related) и 6 high вместе с dev dependencies.
    - Production Telegram chain подтверждён read-only: service active/enabled с 2026-08-11, restart count 0; local/public health OK; порт 8443 слушает; Telegram getMe/getWebhookInfo OK, pending 0, last error отсутствует; SQLite содержит 1 authorized user и 3 messages.
    - Реальный update 2026-08-23 принят, голос прошёл транскрибацию и дошёл до Codex; AI child завершился code 1 с `/system.slice/musikschule-tg-bot.service is not a snap cgroup for tag snap.codex.codex`.
    - Root cause: wrapper запускает сторонний strict Snap `codex` 0.114.0 от `jcat`; тот же systemd-вызов был успешен 2026-08-11, затем 2026-08-13 системный snapd обновился 2.76.1 → 2.76.2 и усилил cgroup tracking. Интерактивный `codex --version` под ubuntu работает, failure специфичен для запуска Snap из non-snap systemd cgroup.
    - Вторичный production-дефект: после Codex failure bot вызвал `git checkout -- .` от root; `/home/ubuntu/Rudolf_music_site/.git/index` теперь `root:root 640`, поэтому `ubuntu` получает `Permission denied`. Worktree по root-проверке чистый; runtime не исправлялся.
    - Сайт публично отвечает HTTP 200 на основные SPA routes через Cloudflare; TLS origin валиден до 2026-10-18; контейнер up 4 weeks; диск EC2 занят на 81% (3.7 GiB free).
    - AWS Console через явно выбранный Chrome: outstanding `$0.00`, payments due `0`; August bill `Pending`, current estimated grand total `$12.92` (tax `$1.78`); MTD `$19.79`; full-month forecast `$26.80`; credit actual remaining `$7.01`, estimated remaining `$0.00`; default Visa valid through 2028-12, backup disabled.
    - Официальные AWS docs: default card списывается автоматически в начале месяца; при пустой Payments due ручное действие не требуется. Клиентская карта может быть добавлена в конкретный AWS account; обычный Amazon retail account не импортирует payment method автоматически.
    - Текущий SherMos2 — standalone AWS account без Organization. Для отдельного payer client AWS management account может пригласить SherMos2 в Organization; после acceptance management account/payment method платит новые charges. Полный ownership transfer и Billing Transfer — отдельные альтернативы; Billing Transfer требует двух management accounts и console flow с Billing Conductor.
    - `llm-verifier-best-of-n`: сформированы 3 независимых сценария, estimate = 18 verifier calls; live tournament не запущен, потому что `llm_verifier`/source checkout и backend credentials отсутствуют. Победитель verifier не заявляется.
    - Свежий read-only preflight 2026-08-23: service active/enabled, wrapper всё ещё вызывает `/snap/bin/codex`; standalone отсутствует; Snap `codex-cli 0.114.0` авторизован; единственный чужой файл в repo — `.git/index` (`root:root 640`); unit работает как root, TLS key доступен только root, поэтому простой перевод всего unit на `ubuntu` без изменения TLS-модели некорректен.
    - Официальный OpenAI installer проверен без запуска на production: Linux standalone ставится в `/home/ubuntu/.local/bin/codex`, актуальный канал на момент проверки — `0.149.0`; текущие флаги бота `exec`, `--ephemeral`, `--sandbox`, `--cd`, `--output-last-message` подтверждены официальной документацией.
    - Рекомендуемый hotfix-порядок валидирован синтаксически: preflight → standalone install pinned 0.149.0 → auth/read-only smoke → backup/stop → atomic wrapper replacement → Git shared ownership → systemd-cgroup smoke → typecheck/start → Telegram text+voice E2E → post-check/rollback. Production не менялся.
    - Для AWS Organizations рекомендовано: отдельный клиентский AWS management account без workloads, all features, MFA/payment/tax setup; invite существующего standalone SherMos2; acceptance ближе к UTC month boundary; management account платит только charges после acceptance, pre-join invoice остаётся текущему payer; invited account не получает автоматический `OrganizationAccountAccessRole`.
    - Текущий `$7.01` credit при присоединении в конце августа остаётся на индивидуальном August bill и может участвовать в shared organization credits с сентября, если account уже состоит в Organization на начало месяца; credit sharing и tax inheritance требуют отдельной проверки/решения.
    - Создан `scripts/recover_codex_production.sh`: повторяемый production-runbook с preflight, root-only backup, error-triggered rollback wrapper/service, pinned official Codex 0.149.0, shared `.git` ownership, systemd-cgroup smoke, typecheck и HTTPS health. Локальный `bash -n` успешен.
    - Первый live-run 2026-08-23: preflight успешен; official standalone Codex 0.149.0 установлен в `/home/ubuntu/.local/bin/codex`; затем `codex login status` вернул `Not logged in`. Script остановился до создания backup, остановки service, изменения wrapper или Git ownership; service был автоматически оставлен/восстановлен active.
    - Запущен device authorization для `ubuntu`; одноразовый code выдан пользователю и ожидает подтверждения (срок 15 минут). После успешного login повторно загрузить актуальный script и запустить его; не считать инцидент исправленным до systemd smoke и Telegram E2E.
    - Вход в standalone Codex не перезапускает Telegram bot, не меняет wrapper/Git/env и не удаляет Snap session; это отдельная user-level ChatGPT auth запись в `/home/ubuntu/.codex`. Device-login по-прежнему ожидает подтверждения на момент 2026-08-24T00:07Z.
    - Device login завершён successfully. Повторный live-run recovery script полностью успешен: official Codex 0.149.0 smoke → OK; wrapper заменён на `/home/ubuntu/.local/bin/codex`; Git `.git` переведён на `ubuntu:ubuntu`, index `660`; systemd-cgroup Codex smoke → OK без Snap error; Telegram bot typecheck прошёл; service restarted active/enabled (`MainPID=175180`, `NRestarts=0`); local и public HTTPS `/health` ответили `ok`.
    - Root-only production backup создан: `/var/backups/musikschule-codex-recovery/20260824T002237Z`. Старый Snap не удалён, его можно вернуть через backup wrapper только как rollback. Реальный Telegram text/voice E2E ещё ожидает действия пользователя.
    - Свежий инцидент 2026-08-30: доверенный телефон `+49 177 7711246` подтверждён в production allowlist; Telegram user id `6327018818` был unauthorized, потому что отправлял обычный text, а не self-contact. В commit `a1c4ce1` reply keyboard авторизации сделана persistent, отменён `one_time_keyboard`, добавлена инструкция/fallback. Локальный и production `npm run typecheck` прошли; commit запушен и fast-forward deployed. Service active/enabled, `NRestarts=0`, local/public health `ok`, startup `setWebhook result: true`. Новое контактное приглашение Telegram доставлено в chat (`message_id=127`) без раскрытия token.
    - Полная переиндексация 2026-09-21: 375 файлов; `music_site_copy/` по-прежнему отсутствует. Production content commit `688fca8` сохранён в origin до разработки.
    - Причина image-сбоя 2026-09-16 подтверждена журналом: у Codex CLI 0.149.0 флаг `--image <FILE>...` поглощал positional prompt, затем stdin был закрыт и CLI возвращал `No prompt provided via stdin`.
    - В commit `73a8778` prompt переведён на stdin; добавлены проверка JPEG/PNG/WebP до 15 MB, ffmpeg→WebP, временное хранение до описания, managed assets `site/public/images/admin/`, image/document input, cleanup на fail/cancel/timeout, image blocks, управляемый homepage hero и argv-based Git без shell-string injection.
    - В commit `20b1af4` добавлено понятное сообщение пользователю при исчерпании Codex quota. Оба commit запушены и развёрнуты; сайт пересобран blue-green, bot перезапущен.
    - Production verification: service active/enabled, `NRestarts=0`, local/public bot health ok, site `/` и `/aktuelles` HTTP 200, Telegram webhook pending 0/last error null, repo clean at `20b1af4`, `.git/index ubuntu:ubuntu 664`. Media pipeline smoke успешен.
    - Установлен Ubuntu package `bubblewrap 0.6.1-1ubuntu0.3`, требуемый Codex workspace sandbox. Image-smoke теперь принимает prompt и attachment, но ChatGPT-auth account отвечает общим `You've hit your usage limit`; Sol и Terra одинаково заблокированы до показанного CLI времени `12:33 PM` либо добавления credits/API billing.
    - Site production audit выявил 2 high advisory в `react-router`/`react-router-dom` (RSC-mode CSRF); текущий сайт — статический SPA без RSC server actions. Bot production audit: 0 vulnerabilities. Автоматический dependency upgrade не выполнялся вне текущего scope.
    - AGY 1.2.7 найден и авторизован под `ubuntu`; direct headless JSON, stream NDJSON, workspace read и isolated write smoke прошли. Выявлено, что одного cwd недостаточно, поэтому site workspace всегда передаётся через `--add-dir`.
    - В commits `f9077af`, `4658d8e`, `517fce3` Codex runtime заменён на AGY: добавлена строгая проверка terminal JSON status, pinned model, sandbox, bounded process-group timeout/output, prompt через stdin, clean-worktree preflight, allowlist для diff и очистка неожиданных untracked files; исправлено форматирование подчёркиваний в идентификаторах.
    - Rollback-safe production migration успешна; backup `/var/backups/musikschule-agy-migration/20260921T162555Z`. `/etc/music_school.env` содержит только `AGY_*` runtime keys, wrapper `/usr/local/bin/music-school-agy` запускает AGY как `ubuntu`; Codex процессов нет.
    - Production verification: service active/enabled, `NRestarts=0`, local/public health `ok`, site HTTP 200, webhook URL совпадает, pending 0, last_error null, repo clean at `517fce3`. Контролируемый AGY write smoke изменил ровно разрешённый `content.js` и был восстановлен.
    - Полный синтетический E2E webhook → authorized user → AGY → Telegram/history успешен; финальный assistant response сохранён ровно как `AGY_E2E_OK`, сайт и worktree не изменились.
    - Инцидент 2026-09-22 подтверждён production-журналом: после истечения первого confirm-timeout root-процесс выполнил Git rollback и оставил `content.js` как `root:root 640`; следующий photo flow создал `site/public/images/admin/` как `root:root 750`. AGY под `ubuntu` не мог читать контент и изображение, но terminal status оставался `SUCCESS`, а пустой diff превращался в ложное `✅ Erledigt.`.
    - Commit `ff21330` сохраняет владельца репозитория для WebP и editable files после root-операций, восстанавливает ownership перед запуском AGY и запрещает success-ответ при image request без реального diff. Изменение запушено и fast-forward развёрнуто.
    - Локально прошли bot typecheck, site lint/build. Production image smoke успешен: WebP `uid=1000`, AGY изменил `content.js`, diff включил content и image, allowlist чист; rollback удалил smoke asset и вернул clean worktree. Service active, `NRestarts=0`, webhook pending 0/last error null, `/health` ok, `/aktuelles` HTTP 200.
  - Now:
    - Подтверждена новая ошибка прав: после confirm 2026-09-23 bot создал WebP `640`; Docker COPY сохранил `640 root:root`, nginx uid 101 отвечал origin HTTP 403. Точечный chmod `644` на production worktree и активном контейнере восстановил origin/public HTTP 200. Пользовательский commit `9c2b1ae` сохранён в GitHub и подтянут локально; требуется постоянная правка media/deploy и проверка.
  - Next:
    - Задать WebP mode 644 при активации, добавить проверку HTTP доступности asset после rebuild, проверить и развернуть bot; затем подтвердить чистоту repo и public HTTP 200.
    - Пользователю повторно отправить исходное фото с подписью и нажать `Подтвердить` в течение пяти минут; удалённое после неудачной попытки изображение восстановить из worktree нельзя.
    - После периода наблюдения решить, удалять ли неиспользуемые Codex binary/wrapper/auth artifacts; сейчас они не участвуют в runtime и оставлены как обратимый fallback.
    - Отдельно обновить `react-router`/`react-router-dom` после проверки совместимости и rebuild; advisory не эксплуатируется текущей статической SPA-архитектурой, но зависимость следует актуализировать.
    - После решения клиента выполнить Organizations change window: export billing history, invite, accept у UTC month boundary, verify payer/tax/credit sharing/budgets и наблюдать 24–48h.
- Open questions (UNCONFIRMED):
  - UNCONFIRMED: финальный пользовательский photo+caption → Telegram confirm → rebuild после исправления; внутренний production image → AGY → diff → rollback smoke успешен.
  - UNCONFIRMED: клиент должен только платить, владеть AWS account целиком или быть management/payer через AWS Organizations.
  - Нужен ли клиенту полноценный mailbox `info@...`; если да, какой провайдер предпочтителен: Google Workspace, Microsoft 365, mailbox.org или другой.
  - Выбирать ли быстрый AssemblyAI restore или сразу реализовать Amazon Transcribe с IAM role/S3 lifecycle.
- Working set:
  - Files: `AGENTS.md`, `AGENT_LOG.md`, `CONTINUITY.md`, `COMMIT_MESSAGE.md`, `INDEX_REPORT.md`, `services/telegram-bot/`, `site/`.
  - Server: `/etc/music_school.env`, `/etc/systemd/system/musikschule-tg-bot.service`, `/usr/local/bin/music-school-agy`, `/home/ubuntu/.local/bin/agy`.
  - Commands: `ssh aws-shermos2-frankfurt`, `systemctl status musikschule-tg-bot`, `npm run typecheck`, `agy --version`, `agy models`.
