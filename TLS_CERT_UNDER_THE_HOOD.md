# TLS_CERT_UNDER_THE_HOOD.md — Полное пособие (математика + ОС + сеть)

Это сводный документ: математические основы, структура сертификатов и то, как это реально выполняется в ОС и сети. Порядок — от абстрактной модели к байтам.

---

## 1) Абстрактная модель (функции и отображения)

Можно представить TLS как композицию функций:

1) **Аутентификация сервера**
   - Вход: домен, цепочка сертификатов, trust store
   - Выход: «доверен/не доверен» + публичный ключ сервера

2) **Общий секрет**
   - Вход: публичные ключи ECDHE
   - Выход: общий секрет `S` (не раскрываемый наблюдателю)

3) **Производные ключи**
   - Вход: `S` + transcript
   - Выход: набор симметричных ключей (client/server)

4) **Шифрование трафика**
   - Вход: ключи + plaintext
   - Выход: ciphertext + тег целостности

Композиция:
```
TLS = Encrypt( HKDF( ECDHE() ), data )
```

---

## 2) Математические основы

### 2.0 Минимум теории групп и полей (без абстракций)
Пусть есть **множество** `G` и **бинарная операция** `⊙ : G × G → G`.  
Группа — это когда выполняются 4 условия:

1) **Замкнутость:** `a ⊙ b ∈ G` для любых `a, b ∈ G`  
2) **Ассоциативность:** `(a ⊙ b) ⊙ c = a ⊙ (b ⊙ c)`  
3) **Нейтральный элемент:** существует `e`, что `a ⊙ e = e ⊙ a = a`  
4) **Обратный элемент:** для каждого `a` существует `a⁻¹`, что `a ⊙ a⁻¹ = e`

Если ещё выполняется **коммутативность**, группа называется **абелевой**.  
**Векторное пространство** — это по сути *абелева группа по сложению* плюс операция умножения на скаляр.  
Поэтому ниже, когда говорится «группа», можно мыслить «конечное множество с операцией, которая ведёт себя по этим 4 правилам».

**Поле** `F` — это множество с двумя операциями `+` и `*`, где:
- `(F, +)` — абелева группа
- `(F \ {0}, *)` — абелева группа
- выполняется дистрибутивность: `a*(b+c) = a*b + a*c`

**Криптография** почти всегда работает в *конечных* множествах:
- конечные поля `F_p` (арифметика по модулю `p`)
- конечные группы точек эллиптических кривых (операция «сложения» точек)

Если термин «группа» мешает, заменяй его мысленно на:  
**«конечное множество + операция, которая соблюдает 4 правила»**.

### 2.1 Группы и ECDHE
- Группа `G` с операцией `⊙`
- Генератор `g`
- Секреты `a`, `b`
- `A = g^a`, `B = g^b`
- Общий секрет `S = B^a = A^b`

### 2.2 Хэширование и HKDF
- HKDF извлекает и расширяет ключ:
```
PRK = HMAC(salt, IKM)
OKM = HMAC(PRK, info || counter)
```

### 2.3 Подписи
Подпись фиксирует факт владения приватным ключом:
```
sig = Sign(sk, H(message))
Verify(pk, H(message), sig) == true
```

---

## 3) Сертификат как бинарная структура

Сертификат — ASN.1 DER структура. PEM — только Base64.

```
Certificate ::= SEQUENCE {
  tbsCertificate,
  signatureAlgorithm,
  signatureValue
}
```

**tbsCertificate** содержит:
- Subject (домен)
- SubjectPublicKeyInfo (публичный ключ)
- Validity
- Extensions (SAN, EKU)

**Подпись** = Sign(CA_key, H(tbsCertificate)).

---

## 4) TLS 1.3 шаг за шагом

1) Клиент: ClientHello + ECDHE share
2) Сервер: ServerHello + ECDHE share
3) Сервер отправляет сертификат
4) Сервер доказывает владение ключом (CertificateVerify)
5) Finished
6) Клиент отвечает Finished

После этого — только шифрованные записи.

---

## 5) Где «сертификат» в причинно‑следственной цепочке

- Без сертификата клиент не знает, с кем говорит.
- Подпись сертификата — математическое доказательство, что домен связан с ключом.
- CertificateVerify — доказательство, что сервер владеет приватным ключом.

---

## 6) ОС и пакеты

### 6.1 Путь данных
1) NIC → драйвер → `sk_buff`
2) IP/TCP → сокет
3) `nginx` читает сокет → OpenSSL
4) OpenSSL расшифровывает → HTTP

### 6.2 Почему шифрование в user space
- OpenSSL — библиотека, а не часть ядра.
- Ядро оперирует байтами, не знает семантики TLS.

---

## 7) Cloudflare как «двойной TLS»

```
Клиент ↔ Cloudflare ↔ Ваш сервер
```

- Клиент проверяет сертификат Cloudflare.
- Cloudflare проверяет сертификат вашего сервера.
- Это две независимые криптографические сессии.

---

Если нужна схема байтовых дампов ClientHello/ServerHello — добавлю.
